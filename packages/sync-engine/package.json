{
  "name": "@prehospital-ems/sync-engine",
  "version": "0.0.0",
  "description": "IndexedDB write queue, background sync worker, and dead-letter handling for prehospital EMS",
  "license": "MPL-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/openmrs/openmrs-ems-prehospital",
    "directory": "packages/sync-engine"
  },
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "dexie": "^3.2.7",
    "@prehospital-ems/fhir-contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.3",
    "typescript": "^5.8.3",
    "fake-indexeddb": "^6.0.0"
  }
}
